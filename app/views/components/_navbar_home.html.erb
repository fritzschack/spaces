<div class="navbar-home">
  <a href="/" class="navbar-home-brand">
    <i class="fas fa-user-astronaut"></i>
  </a>

  <div class="navbar-home-right hidden-xs hidden-sm">
    <% if user_signed_in?  %>
      <%= link_to "Add A Space", new_space_path, :class => 'btn btn-primary add-space-button' %>
    <% end %>
    <div class="navbar-home-item">
      <div class="dropdown">
          <% if user_signed_in? %>
            <p class="avatar dropdown-toggle" id="navbar-home-menu" data-toggle="dropdown">
              <i class="fas fa-user"></i>
            </p>
            <ul class="dropdown-menu dropdown-menu-right navbar-home-dropdown-menu">
              <li><%= link_to "My Profile", my_profile_path %></li>
              <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
            </ul>
          <% else %>
            <%= link_to "Login", new_user_session_path, :class => "navbar-home-item navbar-home-link" %>
            <%= link_to "Create Account", new_user_registration_path, :class => "navbar-home-item navbar-home-link" %>
          <% end %>
      </div>
    </div>
  </div>

  <div class="navbar-home-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fas fa-bars dropdown-toggle" data-toggle="dropdown" role="button"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-home-dropdown-menu">
        <li><a href="#">Host</a></li>
        <li><a href="#">Trips</a></li>
        <li><a href="#">Messagese</a></li>
      </ul>
    </div>
  </div>
</div>

